# Raw Data

Private raw contracts and local annotation uploads are not included in this open-source copy.

Use these optional local directories when rebuilding datasets:

- `data/raw/user_uploads/`
- `data/raw/user_risk_uploads/`

The repository keeps the processed benchmark splits and legal knowledge bases needed by the experiment scripts.
